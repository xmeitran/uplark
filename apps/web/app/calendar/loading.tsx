import { PageLoadingScreen } from "../../src/components/crm-workspace/page-loading-screen";
export default function CalendarLoading() {
  return <PageLoadingScreen title="Đang tải lịch trình..." columns={7} />;
}
